services:
  - type: web
    name: text-to-sql
    env: java
    plan: free
    buildCommand: mvn clean package
    startCommand: java -jar target/text-to-sql-1.0-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false

